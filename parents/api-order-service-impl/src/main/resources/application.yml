server:
  port: 8010
spring:
  application:
    name: app-order
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true

ribbon:
  ReadTimeout: 5000
  ConnectTimeout: 5000